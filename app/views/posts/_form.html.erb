<div class="container">
  <div class="row">
    <div class="col-sm-6 col-sm-offset-3">
    
        <% if @post.errors.any? %>
          <div class="alert alert-danger">
            <ul>
            <% @post.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>
      
      <%= form_for @post do |f| %>
        <div class="form-group">
          <%= f.text_field :title, class: "form-control input-lg", placeholder: "Ingresa el titulo" %>
        </div>
        <div class="form-group">
          <%= f.text_field :body, class: "form-control input-lg", placeholder: "Ingresa el contenido" %>
        </div>
        <div>
          <%= f.submit %>
        </div>
      <% end %>
    </div>
  </div>
</div>